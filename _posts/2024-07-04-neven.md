---
title: "neven"
date: 2024-07-04
categories:
---
```c
#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int i = 0;

    // Asking the user to input the number of even numbers
    printf("Enter the number of even numbers to print: ");

    printf("The first %d even numbers are:\n", );

    // Loop to print the first n even numbers
    for (i = 1; i <= n; i++)
    {
        printf("%d ", 2 * i);
    }

    printf("\n");
    
    ```