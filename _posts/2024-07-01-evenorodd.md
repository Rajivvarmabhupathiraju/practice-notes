---
title: "calculator.c"
date: 2024-06-26
categories:
---

```c

#include <cs50.h>
#include <stdio.h>

int main(void)
{
    //to check whether given numberis odd or even.
    //define the number? 
    //if number is even;
    //Hi, The given number is even.
    //if number is odd?
    //Hello, The given number is odd.

    int number;
    printf("Enter an integer: ");
    printf("to check whether given number is odd or even.\n");
    number = get_int("define the number? ");
    if(n%4 == 0)
    {
        printf("Hi, The given number is even." /n);
        }


    else
    {
        printf("Hi, The given number is odd." /n);
        }

    }

```